{{ UI.clientLink({
    'dataUrl': path(
        'pmanager_default_createview', {
            entityClass: oro_class_name(entity, true),
            entityId: entity.id
    }),
    'aCss': 'no-hash',
    'iCss': 'icon-file-alt',
    'dataId': entity.id,
    'label' : 'ibnab.pmanager.grid_action.getpdf_label'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'getpdf-dialog',
            'dialogOptions' : {
                'title' : 'ibnab.pmanager.dialog.title'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 500,
                 autoResize: true,
                 modal: true,
                 minHeight: 100
            }
        }
    }
}) }}
